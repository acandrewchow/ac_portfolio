// Create a new component, e.g., ParticleTest.js
import React from "react";
import ParticlesBackground from "./ParticlesBackground"; // Adjust the import path

const ParticleTest = () => {
    
  return (
    <div style={{ minHeight: "100vh" }}>
      <ParticlesBackground />
    </div>
  );
};

export default ParticleTest;
